import express from 'express';
// import { streamChat } from '../controllers/chatController';

const router = express.Router();

// GET endpoint for streaming chat responses
// router.get('/chat', streamChat);

export default router;